# StreamSentinel — Frontend Environment Configuration Example
# Copy this file to .env or .env.local for custom deployment configuration.

# Backend FastAPI Base URL (default fallback in code: http://localhost:8000)
# Replace with your deployed backend URL in production (e.g. https://api.streamsentinel.example.com)
VITE_API_URL=https://your-production-backend.example.com
